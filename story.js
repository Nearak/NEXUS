'use strict';
/* ============================================================
   NEXUS-7 — story.js
   ملف القصص والمهام. كل ليلة جديدة تُضاف هنا.
   المحرك (game.js) يقرأ من هذا الملف.
   ============================================================ */

/* ---------- بيانات المشهد الثابت ---------- */
const STORY_META={
  boss:'كامل',            // اسم المشرف
  bossDefault:'كامل',     // اسم مُرسل الرسائل الافتراضي
  codename:'راصد',        // لقب اللاعب
  channel:'CH-07'
};

/* ---------- أسماء الحقنة البرمجية (يستخدمها المحرك) ----------
   ST_HOOKS مصدر الحقيقة الوحيد للقصص. المحرك يستدعي:
   ST_HOOKS.onObj(id)   عند إنجاز هدف
   ST_HOOKS.onEvent(ev) عند أحداث نظام (connect / breach / accept ...)
   كل دالة ترجع true إذا استهلكت الحدث، وإلا يتصرف المحرك افتراضياً.
--------------------------------------------------------------- */
const ST_HOOKS={
  onObj:null,
  onEvent:null
};

/* ---------- سجل الليل ---------- */
const NIGHTS={
  /* كل ليلة: {title, obj:[], hints:{}, intro:[], beats:{...}} */
};
